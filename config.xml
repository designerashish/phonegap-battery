<?xml version='1.0' encoding='utf-8'?>
<widget id="com.designerashish.phoneGap_CorePluginsSamples" version="1.0.0" xmlns="http://www.w3.org/ns/widgets" xmlns:cdv="http://cordova.apache.org/ns/1.0">
    <name>PhoneGap CorePlugin Samples</name>
    <description>
An App. for all phonegap working samples.
    </description>
    <author email="designerashish90@gmail.ccom" href="http://designerashish.org">
        Designerashish
    </author>
    <content src="index.html" />
    <access origin="*" />
    <allow-intent href="http://*/*" />
    <allow-intent href="https://*/*" />
    <allow-intent href="tel:*" />
    <allow-intent href="sms:*" />
    <allow-intent href="mailto:*" />
    <allow-intent href="geo:*" />
    <platform name="android">
        <allow-intent href="market:*" />
    </platform>
    <platform name="ios">
        <allow-intent href="itms:*" />
        <allow-intent href="itms-apps:*" />
    </platform>

    <preference name="orientation"                value="portrait" />        <!-- all: default means both landscape and portrait are enabled -->
    <preference name="target-device"              value="universal" />      <!-- all: possible values handset, tablet, or universal -->
    <preference name="fullscreen"                 value="true" />           <!-- all: hides the status bar at the top of the screen -->
    <preference name="webviewbounce"              value="true" />           <!-- ios: control whether the screen 'bounces' when scrolled beyond the top -->
    <preference name="prerendered-icon"           value="false" />           <!-- ios: if icon is prerendered, iOS will not apply it's gloss to the app's icon on the user's home screen -->
    <preference name="stay-in-webview"            value="false" />          <!-- ios: external links should open in the default browser, 'true' would use the webview the app lives in -->
    <preference name="ios-statusbarstyle"         value="black-opaque" />   <!-- ios: black-translucent will appear black because the PhoneGap webview doesn't go beneath the status bar -->
    <preference name="detect-data-types"          value="true" />           <!-- ios: controls whether data types (such as phone no. and dates) are automatically turned into links by the system -->
    <preference name="exit-on-suspend"            value="true" />          <!-- ios: if set to true, app will terminate when home button is pressed -->
    <preference name="show-splash-screen-spinner" value="false" />           <!-- ios: if set to false, the spinner won't appear on the splash screen during app loading -->
    <preference name="auto-hide-splash-screen"    value="true" />           <!-- ios: if set to false, the splash screen must be hidden using a JavaScript API -->
    <preference name="disable-cursor"             value="false" />          <!-- blackberry: prevents a mouse-icon/cursor from being displayed on the app -->
    <preference name="android-minSdkVersion"      value="11" />              <!-- android: MIN SDK version supported on the target device. MAX version is blank by default. -->
    <preference name="android-installLocation"    value="auto" />           <!-- android: app install location. 'auto' will choose. 'internalOnly' is device memory. 'preferExternal' is SDCard. -->

    <!-- Usage -->
    <preference name="permissions" value="none"/>

    
    <!-- Define app icon for each platform. -->
    <icon src="icon.png" width="512" height="512" />
    <icon src="res/screen/android/512.png"   gap:platform="android"    gap:density="ldpi" />
    <icon src="res/screen/android/512.png"   gap:platform="android"    gap:density="mdpi" />
    <icon src="res/screen/android/512.png"   gap:platform="android"    gap:density="hdpi" />
    <icon src="res/screen/android/512.png"  gap:platform="android"    gap:density="xhdpi" />
    
    <!-- Define app splash screen for each platform.     -->
    <splash src="res/screen/android/ldpi/splash.png"  gap:platform="android" gap:density="ldpi" />
    <splash src="res/screen/android/mdpi/splash.png"  gap:platform="android" gap:density="mdpi" />
    <splash src="res/screen/android/hdpi/splash.png"  gap:platform="android" gap:density="hdpi" />
    <splash src="res/screen/android/xhdpi/splash.png" gap:platform="android" gap:density="xhdpi" />
   
  


    <engine name="android" spec="^6.2.3" />
    <!-- Plugins can also be added here. -->
    <plugin name="cordova-plugin-whitelist" spec="^1.3.2" />
    <plugin name="cordova-plugin-flashlight" spec="^3.2.0" />
    <plugin name="cordova-plugin-battery-status" spec="^1.2.4" />
    <plugin name="phonegap-plugin-barcodescanner" spec="^6.0.8" />
</widget>
